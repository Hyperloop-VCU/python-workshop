""" 3.3 - fibonacci sequence in a list
    Write a function that receives a list as input and appends a new element to the list.
    The new element should be equal to the sum of the last two elements.
    You can assume the list always has at least two elements in it.
    
    hint: you can access the last and second-to-last elements easily like this:
         theList[-1]
         theList[-2]
"""

def append_next_fib(fibList):
    pass # pass means "do nothing" - replace it with your code


### do not modify below this line ###
fibSequence = [0, 1]
append_next_fib(fibSequence)
append_next_fib(fibSequence)
append_next_fib(fibSequence)
append_next_fib(fibSequence)
append_next_fib(fibSequence)
append_next_fib(fibSequence)

print(fibSequence)